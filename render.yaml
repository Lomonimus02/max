services:
  - type: web
    name: consulting-website
    env: static
    buildCommand: npm run build
    startCommand: npm start
    staticPublishPath: ./
    envVars:
      - key: NODE_ENV
        value: production

